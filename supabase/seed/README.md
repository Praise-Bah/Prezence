# Seed Data

Place SQL seed files here for local/dev database seeding.

Run with:
```
supabase db query --linked < supabase/seed/<file>.sql
```

Do not include real user data or secrets in seed files.
